# Godot 4 GDScript API Reference — Index

This index lists all available GDScript-specific Godot 4 API skill files.

Load the relevant skill file(s) when answering questions about Godot GDScript development.


## Skill Files

- **Animation** (`godot_gdscript_animation.md`) — 18 classes, ~4,105 tokens
- **Audio** (`godot_gdscript_audio.md`) — 54 classes, ~7,741 tokens
- **Core** (`godot_gdscript_core.md`) — 39 classes, ~9,095 tokens
- **Editor** (`godot_gdscript_editor.md`) — 53 classes, ~15,570 tokens
- **Filesystem** (`godot_gdscript_filesystem.md`) — 9 classes, ~4,482 tokens
- **Input** (`godot_gdscript_input.md`) — 18 classes, ~2,620 tokens
- **Math Types** (`godot_gdscript_math_types.md`) — 11 classes, ~4,355 tokens
- **Misc Part1** (`godot_gdscript_misc_part1.md`) — 257 classes, ~55,005 tokens
- **Misc Part2** (`godot_gdscript_misc_part2.md`) — 163 classes, ~33,672 tokens
- **Networking** (`godot_gdscript_networking.md`) — 34 classes, ~8,347 tokens
- **Nodes 2D** (`godot_gdscript_nodes_2d.md`) — 43 classes, ~12,383 tokens
- **Nodes 3D** (`godot_gdscript_nodes_3d.md`) — 39 classes, ~10,165 tokens
- **Physics** (`godot_gdscript_physics.md`) — 40 classes, ~12,891 tokens
- **Rendering** (`godot_gdscript_rendering.md`) — 148 classes, ~19,303 tokens
- **Resources** (`godot_gdscript_resources.md`) — 85 classes, ~21,204 tokens
- **Ui Controls** (`godot_gdscript_ui_controls.md`) — 54 classes, ~17,400 tokens

## Usage

When a user asks about a Godot class or concept, identify the domain and load the appropriate file.
All method and property names are snake_case (GDScript convention).
Types use GDScript built-in names (e.g., `String`, `int`, `float`, `Array`, `Dictionary`, `Callable`, `Variant`).
